export const MetadataTypes = {
  Prefix: Symbol.for('PREFIX'),
  Routes: Symbol.for('ROUTES'),
  Params: Symbol.for('PARAMS'),
};

export const ContainerTypes = {
  Controller: 'TYPEDI-EXPRESS-UTILS:CONTROLLER',
};

export const controllers = new Set();
